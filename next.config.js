/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // optional: basePath agar aap subfolder me deploy kar rahe hain
  // basePath: "",
};

module.exports = nextConfig;
